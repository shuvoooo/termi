// This page is intentionally empty.
// The LocalTerminalPanel is always mounted in the dashboard layout
// (similar to the sessions workspace) so the PTY session persists
// while navigating between pages.
export default function LocalTerminalPage() {
    return null;
}
